{{- if not .ephemeral -}}
legacy_version_file = false
experimental = false
{{ if .work -}}
trusted_config_paths = ["~/work"]
{{- end }}
raw = true

python_venv_auto_create = false
python_compile = true
{{ end }}
