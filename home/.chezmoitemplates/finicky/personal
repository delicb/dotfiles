const DEFAULT: BrowserSpecification = { name: "Firefox" };

export default {
    defaultBrowser: DEFAULT,

    rewrite: [
        {
            match: NON_LOCAL_HTTP_URL,
            url: ENFORCE_HTTPS,
        },
        {
            match: ALL_URLS,
            url: stripTrackingParams,
        },
    ],
} satisfies FinickyConfig;
